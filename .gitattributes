# Normalize line endings to LF across the whole repository.
# Overrides any local core.autocrlf setting so every machine and CI
# sees identical content — avoids spurious "modified" files.
* text=auto eol=lf
